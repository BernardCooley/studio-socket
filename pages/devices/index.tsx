import React from "react";

interface Props {}

const Devices = ({}: Props) => {
    return (
        <div className="devices" data-testid="devices-page">
            Devices
        </div>
    );
};

export default Devices;
