import React from "react";

interface Props {}

const Account = ({}: Props) => {
    return (
        <div className="account" data-testid="account-page">
            Account
        </div>
    );
};

export default Account;
